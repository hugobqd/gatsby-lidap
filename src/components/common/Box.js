import styled from "styled-components";
import {
  color,
  space,
  layout,
  typography,
  flexbox,
  position,
  border
} from "styled-system";

const Box = styled.div`
  ${color}
  ${space}
  ${layout}
  ${position}
  ${flexbox}
  ${typography}
  ${border}
`;

export default Box;
