import styled from "styled-components";
import {
  color,
  space,
  layout,
  typography,
  flexbox,
  position,
} from "styled-system";

const Box = styled.div`
  ${color}
  ${space}
  ${layout}
  ${position}
  ${flexbox}
  ${typography}
`;

export default Box;
