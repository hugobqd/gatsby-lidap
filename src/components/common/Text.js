import styled from "styled-components";
import { typography, layout, space, color } from "styled-system";

const Text = styled.p`
  ${typography}
  ${layout}
  ${space}
  ${color}
`;

export default Text;
